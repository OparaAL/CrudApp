@model CrudApp.Models.Department
@{
    ViewBag.Title = "Delete department";
}

<p>@ViewBag.ErrorDelete</p>
<div>
    <dl class="dl-horizontal">
        <dt>Name</dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>Code</dt>
        <dd>
            @Html.DisplayFor(model => model.Code)
        </dd>
    </dl>

    <div>
        <form asp-controller="Department" asp-action="DeleteAnyway" method="post">
            <div class="form-group">
                <input type="submit" class="btn btn-default" value="Delete anyway" />
            </div>
        </form>
        <form asp-controller="Department" asp-action="Index" method="get">
            <div class="form-group">
                <input type="submit" class="btn btn-default" value="Cancel" />
            </div>
        </form>
    </div>
</div>